#Indicaciones
Para el desarrollo ingorar la instalacion de requeriments.tex mejor hacerlo paso por paso para ello se hace de la siguiente manera:

'''bash

pip install python-dotenv==1.0.1

pip install pydantic==2.10.4

pip install email-validator==2.2.0

pip install pyjwt==2.10.1

pip install bcrypt==4.2.1

pip install sqlalchemy==2.0.36

pip install psycopg2-binary==2.9.10

pip install alembic==1.14.0

pip install fastapi==0.115.6

pip install uvicorn==0.34.0